# .env.local — never commit this file (see CLAUDE.md §11)

# NextAuth
NEXTAUTH_URL=
NEXTAUTH_SECRET=

# LinkedIn OAuth
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# Access code fallback
NEXTAUTH_ACCESS_CODE=

# Vercel KV (session logging)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Airtable (Auth Log) — PAT needs data.records:read + data.records:write on HITCHBASE
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=
