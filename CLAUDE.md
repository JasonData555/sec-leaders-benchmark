# CLAUDE.md — Hitch Intelligence: The Security Leadership Benchmark

Authoritative build reference for every session. Read fully before writing code; follow exactly.
Design system lives in **DESIGN.md**; current state of play in **progress.md**.

---

## 1. Agent behavior (non-negotiable)
- **Subagents:** offload research/exploration/parallel analysis. One task per subagent.
- **Ask, don't assume:** unclear architecture/intent/field names → ask first. State inferences before acting. One question at a time.
- **Simplicity first:** minimum thing that works. No unrequested abstractions, helpers, or "while I'm here" refactors.
- **Surgical changes:** touch only what the task needs. No reformatting/renaming in passing. Minimal diff, minimal blast radius.
- **Flag uncertainty** explicitly. Find root causes, no temporary patches. Senior-dev standards.
- **Commit/push only when asked.**

## 2. Project overview
- **Product:** Hitch Intelligence — The Security Leadership Benchmark. Client-facing comp + governance benchmarking for security leaders.
- **URL:** intelligence.hitchpartners.com · **Hosting:** Vercel (push to `main` auto-deploys).
- **Stack:** Next.js 14 (App Router, TS) · static JSON · NextAuth (JWT) · Puppeteer (PDF). No Tailwind. No DB.
- **Theme:** dark enterprise — Hitch navy/gold with a cobalt data accent.

## 3. Repository map
```
app/
  page.tsx                # Auth gate (LinkedIn + access code)
  benchmark/page.tsx      # Main tool — single page, no scroll
  benchmark/FilterContext.tsx  # filter state + derived flags (nCount, noResults, lowSample, floorWarning, candidate)
  export/page.tsx         # PDF render target (no layout chrome)
  api/{auth,export,log}/  # NextAuth · Puppeteer PDF · session log
components/
  layout/   ToolHeader, ToolFooter, PeerGroupPanel, ZoneStack, ExportButton
  zones/    CompensationZone, GovernanceZone, FunctionalScopeZone  (charts rendered INLINE here)
  filters/  RoleFilter, LocationFilter (retained, not in UI) · others
  ui/       Chip (size md|lg), NCounter, CandidateBand, FloorWarning, CompareForm
lib/        data.ts · filters.ts · metrics.ts · scripts/csv-to-json.ts · auth.ts
data/       allsec-benchmark.json   # source of truth (CISO-only, ~957 records)
public/     hitch-logo.png · hitch-logo-white.png (white used on the dark theme)
styles/     globals.css             # all design tokens — never hardcode hex in components
```
> `components/charts/*` are empty stubs — charts live inline in the zone files as custom SVG/CSS (not Recharts).

## 4. Design system (see DESIGN.md)
- **Never hardcode hex in components.** All colors are CSS custom properties in `styles/globals.css` (`:root`). Change values there.
- Current core tokens: `--ink-deep #0B1F3A`, `--ink #0D1B2E`, `--ink-surface #122338`, `--champagne #C9A44C`, `--champagne-mid #A8873A`, `--text-primary #F7F9FC`, `--data-cobalt #2F80ED` (signal accent), `--hitch-blue #2F80ED` (auth LinkedIn button ONLY).
- **Fonts** (loaded in `app/layout.tsx`): Cormorant Garamond (display/numbers), DM Sans (UI copy), IBM Plex Mono (labels/percentages/ranges/metadata).
- Border-radius max: 10px outer container, 3–5px cards/charts, 2px chips/buttons. Shadows use `rgba(0,0,0,n)` only.

## 5. Data layer
- `data/allsec-benchmark.json` — **CISO-only, ~957 records**. Generated by `lib/scripts/csv-to-json.ts` (filters `Role_Bucket === "CISO"`) from `AllSecBenchmark-highcon - CISO.csv`. Run manually; not part of build.
- Loaded once (`lib/data.ts`); all filtering client-side, additive AND logic (`lib/filters.ts` → `applyFilters`).
- **Primary cohort split:** `Industry Tier` = `Baseline` | `High Consequence` (top filter, enlarged pills). Company Size secondary. Role/Location plumbing retained but pulled from UI.
- Key fields: `Base/Bonus/Equity/Total Comp-Converted` (parse via `parseComp`), `How often do you present to the Board of Directors?`, `Are you currently included in the following?` (D&O), severance/vesting Yes/No, `Team Size`.

## 6. Metrics (`lib/metrics.ts` — no calc logic in components)
- `percentile()` (P25/P50/P75 markers), `parseComp()`, `classifyDO()`, board/severance/vesting rates.
- **Headline Total Comp = sum of base+bonus+equity averages** (`totalCompAvg`), NOT the mean of the Total Comp column. This dataset intentionally shows **averages**, deviating from the legacy "median never mean" rule — keep as-is unless stakeholders change it.
- Display rules: `$XXXK` (round to $1K), whole-number `%`, nulls excluded never imputed 0.

## 7. Layout
- Single-page, **no-scroll** desktop (≥1280px): 100vh, overflow hidden. Column: header → body → footer.
- Body row: left panel (236px fixed) + right content (flex). Right = 3 zones; zones own padding `20px 26px`, divided by 1px border. Candidate band conditional above footer.
- Tablet (768–1279): panel → horizontal bar, zones stack, page scrolls. Mobile (<768): bottom-sheet filters, governance 2×2.

## 8. Auth
- `app/api/auth/[...nextauth]/route.ts` + `lib/auth.ts`. Providers: LinkedIn OAuth (`openid profile email`) + Credentials (shared `NEXTAUTH_ACCESS_CODE`). JWT session, 8h max.
- On login POST `/api/log` (timestamp, provider, name/title/company, sessionId). Session-level only.
- Auth screen: `--ink-deep` full viewport, centered max-w 400px, white logo, LinkedIn button (`--hitch-blue`), divider, access-code input, methodology footnote.

## 9. PDF export
- `app/api/export/route.ts`: builds `/export?…` URL from peer-group params, launches Puppeteer (`@sparticuz/chromium` on Vercel), waits for render, prints Letter portrait no-margin.
- `app/export/page.tsx`: same three zones, no header/footer chrome, dark cover strip (white logo · product name · date + peer summary `n=X`), methodology footer.

## 10. Environment (`.env.local`, never commit)
`NEXTAUTH_URL` · `NEXTAUTH_SECRET` · `LINKEDIN_CLIENT_ID/SECRET` · `NEXTAUTH_ACCESS_CODE` · `KV_URL` + `KV_REST_API_*` (session log).

## 11. Vercel & DNS
- Project: Next.js framework, root `/`, build `next build`, Node 20.x. Push to `main` auto-deploys.
- DNS: `CNAME intelligence → cname.vercel-dns.com` (Squarespace apex untouched).
- LinkedIn redirect URIs: `https://intelligence.hitchpartners.com/api/auth/callback/linkedin` + `http://localhost:3000/...`.

## 12. Data update (annual, manual)
New cohort CSV → synthesis pipeline → retire oldest cohort → `csv-to-json.ts` → replace JSON → update vintage label (`2025–2026 survey period`) → redeploy. Confirm before deploy.

## 13. What this is NOT
Not DB-backed (static JSON only) · not real-time · not public (auth on every route) · not multi-tenant · not search (filters only) · not a scrolling dashboard (no tabs/modals/nav) · not Tailwind · three chart types only (distribution bar, board donut, horizontal function bars).

## 14. Future Hitch Intelligence products
This file is the fork template; DESIGN.md inherited wholesale (only wordmark string changes). Core color tokens and auth-only `--hitch-blue` stay constant. New products may add one champagne-derived data accent.

---
_Last updated: 2026-06-24 · v1.0 · Next.js · Vercel · NextAuth · Puppeteer_
